apiVersion: gateway.networking.k8s.io/v1beta1
kind: HTTPRoute
metadata:
  name: hugo
spec:
  parentRefs:
  - name: traefik-gateway
    namespace: kube-system
    sectionName: web
  hostnames:
  - blog.andrewlee.fun
  rules:
  - backendRefs:
    - name: hugo
      port: 80
